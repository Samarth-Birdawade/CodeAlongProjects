"""
Requirements.txt has all the packages listed that I have used in the project.

Open the folder in File Explorer then type 'cmd' in the address bar of the window and then execute the following command to activate the virutal environment:

.venv_for_codealong\Scripts\activate

To run the streamlit file - paste the following in the command line with the address of your Streamlit.py:
streamlit run <address>/Streamlit.py

"""